# Wikidata 人物关系图职业预测：PPT 大纲

> 版本：基于 `runs_report/` 的三 seed 汇总结果（seed 42 / 43 / 44）。
>
> 建议时长：15–20 分钟，正文 16 页，附录 4–5 页。
>
> 汇报主线：**图关系和邻居职业信息确实有效；但人物关系图高度稀疏、碎片化，职业信号覆盖不均，使 L3 细粒度职业预测受到数据上限限制。**

---

## 正文

## 1. 数据从哪里来？项目交接与图构建

**项目交接：**

- 博士师姐已完成 Wikidata 人物关系数据导出，并用 R-GCN 做过初步职业预测实验；
- 当前工作在该项目目标和原始数据基础上继续：统一数据处理、补严防泄漏协议，并系统比较关系语义、输入信息、模型和数据覆盖；
- 主任务保持为人物关系图上的职业节点分类；不把预测结果解释为“职业继承”或因果影响。

**原始文件：** `Q_R_Q_extended.txt`，来自 Wikidata 的人物关系边中心导出。

**每一行包含：**

```text
Node1（人物） , Relation , Node2（人物）
Node1 / Node2 的出生、死亡、国家、职业 Level 1 / 2 / 3
```

**预处理：**

1. 从重复出现的端点属性中规范化出唯一人物表；
2. 将文本缺失值（`Missing`、`Unknown`、空值等）统一为真实缺失；
3. 记录同一人物重复属性不一致的审计表；
4. 保留所有人物和原始关系边；
5. 为每个原始关系补一条显式反向边，允许消息双向传播；
6. 对 `(source, relation, target)` 去重，导出可复现的 PyG artifact。

**规模：**

| 项目 | 数值 |
|---|---:|
| 人物节点 | 334,099 |
| 原始人物—人物边 | 691,503 |
| 加反向边、去重后的有向边 | 1,383,006 |
| 关系类型（含反向） | 80 |

**建议图：** 原始 CSV → 人物表 + typed edge table → PyG `graph_data.pt` 的流程图。

---

## 2. 任务定义：层级职业节点分类

每个人预测一个保留的职业类别，三个层级**分别准备数据、分别训练、分别评估**：

| 层级 | 含义 | 难度 |
|---|---|---|
| Level 1 | 粗粒度职业大类 | 低 |
| Level 2 | 中等粒度职业类别 | 中 |
| Level 3 | 细粒度职业类别 | 高，长尾最显著 |

以 Level 3 为例，默认只保留训练样本数不少于 20 的类别，共 682 类；低频职业和无标签人物仍作为图中的邻居保留，但不参与监督 loss 或测试指标。

形式化表示：

\[
G=(V,E,\mathcal R), \qquad e=(u,r,v),\ r\in\mathcal R
\]

对每个可监督人物 \(v\)，预测：

\[
\hat y_v=\arg\max_{c\in\{1,\ldots,C\}} p(y_v=c\mid G,X)
\]

---

## 3. 如何划分数据？如何防止职业标签泄漏？

**划分：**

- 每个职业层级独立按目标职业分层；
- train / validation / test = **70% / 10% / 20%**；
- 类别至少在三个 split 各保留一个样本；
- 本轮报告固定 artifact，仅改变训练随机 seed（42、43、44），不改变数据划分。

**关键防泄漏协议：**

| 人物角色 | L1/L2/L3 职业特征是否可见？ |
|---|---|
| 训练人物作为邻居 | 可见 |
| 当前 forward 的训练 seed | 临时全部 mask 为 `UNKNOWN` |
| validation 人物 | 全部为 `UNKNOWN` |
| test 人物 | 全部为 `UNKNOWN` |
| 人物—人物关系边 | 始终保留 |

因此，模型可以利用**其他已知训练人物**的职业，但不能读取待预测人物自身的职业。

**建议图：** 同一张图中用颜色区分训练邻居、当前 seed、验证、测试，并标注职业遮蔽状态。

---

## 4. 节点特征与初始表示

**主线输入：**

| 特征 | 编码方式 | 可见性 |
|---|---|---|
| 邻居职业 L1/L2/L3 | 独立类别 embedding | 按防泄漏协议遮蔽 |
| 国家 | 类别 embedding | 所有节点可见 |
| 时间 | 出生年、死亡年、年龄、缺失标记 | 所有节点可见 |

每种特征先进入独立编码分支，再经过 gate 融合：

\[
z_{v,j}=f_j(x_{v,j}),\qquad
\alpha_v=\operatorname{softmax}(W_g[z_{v,1};\ldots;z_{v,m}])
\]

\[
h^{(0)}_v=\operatorname{MLP}([\alpha_{v,1}z_{v,1};\ldots;\alpha_{v,m}z_{v,m}])
\]

其中 \(f_j\) 是类别 embedding、数值 MLP 或固定语义向量的投影层。

**建议图：** L1/L2/L3、country、temporal 五个分支 → feature gate → fused node embedding。

---

## 5. 三种关系 GNN：共同形式

所有模型都做两层关系型消息传递，并使用残差、LayerNorm 与 Dropout：

\[
h^{(\ell+1)}_v=\operatorname{LayerNorm}\left(
h^{(\ell)}_v + \operatorname{Dropout}(\operatorname{GELU}(m^{(\ell)}_v))
\right)
\]

最后以线性分类器与 softmax 得到职业概率：

\[
p_v=\operatorname{softmax}(W_{\text{cls}}h_v^{(2)}+b)
\]

区别在于如何计算 relation-aware message \(m_v^{(\ell)}\)。

---

## 6. 模型一：R-GCN

R-GCN 为每种关系学习不同的消息变换：

\[
m^{(\ell)}_v=
\sum_{r\in\mathcal R}
\sum_{u\in\mathcal N_r(v)}
\frac{1}{c_{v,r}}W_r^{(\ell)}h_u^{(\ell)}
\]

\[
h_v^{(\ell+1)}=
\sigma\left(W_0^{(\ell)}h_v^{(\ell)}+m_v^{(\ell)}\right)
\]

**本项目实现：** FastRGCNConv；以更高显存占用换取更快训练速度，并可使用 basis decomposition 降低参数规模。

**角色：** 最稳定、最快的主对照模型；绝大多数 L3 消融固定使用它。

---

## 7. 模型二：R-GAT 与模型三：CompGCN

### R-GAT：关系感知注意力

对同一目标节点的不同邻居学习注意力权重：

\[
\alpha_{u,v,r}=
\operatorname{softmax}_{u\in\mathcal N(v)}
\left(a_r(W h_u, W h_v)\right)
\]

\[
m_v=\sum_{(u,r,v)\in E}\alpha_{u,v,r}W_r h_u
\]

注意力只可作为候选解释信号；它不等于社会科学意义上的因果重要性。

### CompGCN：组合节点与关系表示

每种关系有 embedding \(e_r\)，先将源节点与关系组合：

\[
m_{u\rightarrow v}=W\left(h_u\odot e_r\right)
\]

\[
h'_v=W_{\text{root}}h_v+
\operatorname{Mean}_{(u,r,v)\in E}m_{u\rightarrow v}
\]

本轮使用逐元素乘法 \(\odot\)；关系 embedding 也会在每层更新。

---

## 8. 训练与评估 pipeline

**训练：**

```text
监督 seed 人物
  ↓ NeighborLoader 采样两跳子图（默认 fan-out 15, 10）
  ↓ 当前 batch seed 的职业特征设为 UNKNOWN
  ↓ 特征编码与融合
  ↓ 两层 R-GCN / R-GAT / CompGCN
  ↓ 仅对 batch 前 batch_size 个 seed 计算 CrossEntropy
  ↓ AdamW + 梯度裁剪 + ReduceLROnPlateau
```

**模型选择与测试：**

- 本轮汇报矩阵按 validation Macro-F1 保存最优 checkpoint；
- early stopping：`patience=6`，`min_delta=0.001`；
- 每个 seed 的 test 只在 validation 选定 checkpoint 后评估一次；
- 指标：Accuracy、Macro-F1、Weighted-F1；L3 重点看 Macro-F1。

---

## 9. 实验设计：如何保证比较清晰？

**统一原则：**

- 同一层级内读取同一份 `graph_data.pt`；
- 每个条件运行 seed 42 / 43 / 44；
- 输出在 `runs_report/level{1,2,3}/`，不与历史探索性 `runs/` 混合；
- 报告 mean ± sample std，而不是挑选最佳 test；
- L3 的大多数消融固定 Fast R-GCN，确保一次只改变一个因素。

**L3 重跑矩阵：**

1. 模型：R-GCN / R-GAT / CompGCN；
2. 输入：纯结构、L3-only、完整层级、普通属性、固定语义职业向量；
3. 关系：relation type 置乱、亲属/教育关系消融及等规模随机删边对照；
4. 长尾：class-balanced loss、logit adjustment、balanced seed sampling；
5. 覆盖：完整一二跳邻域；full-batch structural 作为附录探索。

---

## 10. 主结果一：职业粒度与模型选择

**表格标题建议：** 不同职业层级的三 seed 测试结果（Accuracy / Macro-F1，%）

| 层级 | R-GCN | R-GAT | CompGCN | 结论 |
|---|---|---|---|---|
| L1 | 78.56 ± 0.07 / 66.44 ± 0.25 | 78.52 ± 0.18 / 66.48 ± 0.27 | 78.58 ± 0.04 / 66.37 ± 0.18 | 三者无决定性差异 |
| L2 | 67.37 ± 0.06 / 45.98 ± 0.46 | 67.14 ± 0.14 / 45.31 ± 0.34 | 67.26 ± 0.06 / 46.62 ± 0.28 | CompGCN Macro-F1 略高 |
| L3 | 43.16 ± 0.04 / 14.82 ± 0.24 | 42.53 ± 0.12 / 14.00 ± 0.06 | 43.03 ± 0.07 / 15.43 ± 0.32 | 模型差异有限，L3 长尾困难 |

**讲述要点：**

- 粒度由 L1 → L3 时，性能大幅下降；细粒度职业是主要难点。
- 三种关系 GNN 的差异远小于 L1/L2/L3 粒度差异。
- 因此后续重点不应只是继续替换 GNN 层。

**建议图：** 横轴 L1/L2/L3，纵轴 Accuracy 与 Macro-F1 的分组柱状图；每根柱画 3 seed 标准差。

---

## 11. 主结果二：职业输入与普通属性

**固定模型：** Fast R-GCN，L3，关系 + 职业输入；除特殊说明外不使用 country/temporal。

| 条件 | Accuracy (%) | Macro-F1 (%) | 相对 R-GCN 基线的解读 |
|---|---:|---:|---|
| 完整 L1+L2+L3 职业层级 | 43.16 ± 0.04 | 14.82 ± 0.24 | 基线 |
| 纯结构（无职业、国家、时间） | 24.84 ± 0.04 | 1.07 ± 0.08 | 邻居职业是最主要信号 |
| 只用邻居 L3 职业 | 43.03 ± 0.05 | 14.79 ± 0.09 | L1/L2 几乎不带来额外增益 |
| 完整层级 + country/temporal | 44.06 ± 0.12 | 15.21 ± 0.26 | 普通属性有稳定但有限增益 |
| 固定 E5 职业语义向量 | 43.02 ± 0.06 | 13.37 ± 0.15 | 名称文本语义未改善，Macro-F1 降低 |

**结论：**

> 可见邻居职业信息有效；职业层级文本化或仅增加上层职业并不能解决 L3 平台。country/temporal 贡献有限，不能视为主要瓶颈。

---

## 12. 主结果三：关系类型的语义价值

| 条件 | Accuracy (%) | Macro-F1 (%) | 结论 |
|---|---:|---:|---|
| R-GCN 基线 | 43.16 ± 0.04 | 14.82 ± 0.24 | 参照 |
| relation type 全局置乱 | 40.89 ± 0.18 | 13.24 ± 0.10 | 拓扑不变但关系语义破坏后下降，relation type 有真实价值 |
| 删除教育/师承关系 | 35.60 ± 0.07 | 11.54 ± 0.09 | 显著下降 |
| 等规模随机删边（教育规模） | 40.58 ± 0.06 | 13.45 ± 0.34 | 教育/师承关系具有超出连通性本身的语义贡献 |
| 删除亲属关系 | 27.47 ± 0.02 | 5.85 ± 0.12 | 大幅下降 |
| 等规模随机删边（亲属规模） | 27.85 ± 0.11 | 7.68 ± 0.30 | 主要损失可由大规模删边解释；亲属特异性需谨慎表述 |

**讲述要点：**

- 置乱是“保留边、破坏类型语义”的控制；证明 relation type 不只是额外参数。
- 教育/师承消融比同等随机删边额外低约 4.99pp Accuracy，说明该类关系有独立职业语义。
- 亲属边占图中大量连接；删除它造成的主要影响是信息/连通性损失，不能直接解释为“亲属职业传递”。

---

## 13. 主结果四：长尾优化与邻域覆盖

### 长尾方法未带来改善

| 条件 | Accuracy (%) | Macro-F1 (%) |
|---|---:|---:|
| R-GCN 基线 | 43.16 ± 0.04 | 14.82 ± 0.24 |
| class-balanced loss | 21.39 ± 0.34 | 10.37 ± 0.22 |
| logit-adjusted loss | 22.27 ± 0.40 | 11.98 ± 0.11 |
| class-balanced seed sampling | 1.41 ± 0.10 | 0.48 ± 0.01 |

### 扩大两跳邻域也没有突破

| CompGCN 条件 | Accuracy (%) | Macro-F1 (%) |
|---|---:|---:|
| 默认 fan-out 15,10 | 43.03 ± 0.07 | 15.43 ± 0.32 |
| 完整一跳 + 二跳邻域 | 43.15 ± 0.02 | 15.62 ± 0.23 |

**结论：**

> 当前平台不主要由 15/10 邻居采样遗漏或通用长尾 loss 引起。

---

## 14. 数据诊断：瓶颈是“关系与职业信息覆盖”

**图结构诊断：**

| 指标 | 数值 | 含义 |
|---|---:|---|
| 不同人物对连接 | 392,724 | 去除方向、关系类型和重复对后的独特邻居关系 |
| 平均不同邻居数 | 约 2.35 | 整体图稀疏 |
| 不同邻居数中位数 | 1 | 至少一半人物仅有一个不同邻居 |
| 最大连通分量占比 | 40.9% | 图高度碎片化 |
| 连通分量数 | 71,838 | 许多小组件无法互相传递信息 |

**测试人物的可见职业覆盖：**

| 条件 | 节点数 | Accuracy | Macro-F1 |
|---|---:|---:|---:|
| 两跳内无可见训练职业 | 7,373 | 18.5% | 0.57% |
| 两跳内有可见训练职业 | 57,594 | 46.3% | 16.1% |
| 直接可见职业邻居为 0 | 12,589 | 27.2% | 6.5% |
| 直接可见职业邻居为 1 | 5,327 | 50.4% | 16.8% |

**建议图：**

- 左：不同邻居数的直方图或分桶柱状图；
- 右：`0 / 1 / 2 / 3–5 / 6+` 可见职业邻居数对应的 Accuracy 与 Macro-F1。

---

## 15. 数据诊断：关系质量并不低

若人物职业独立，训练集中随机两人同属一个 L3 职业的概率仅为 **2.72%**。

| 关系组 | 有标签训练关系对 | 同 L3 职业率 | 相对独立基线 lift | 解释 |
|---|---:|---:|---:|---|
| 教育/师承 | 50,468 | 59.6% | 21.9× | 最强、最稳定的职业关联之一 |
| 职业合作 | 2,695 | 56.6% | 20.8× | 样本较少但关联很强 |
| 宗教关系 | 22,802 | 55.7% | 20.5× | 同职业率高，但互信息较低，可能集中于少数职业 |
| 影响/继任 | 6,878 | 41.2% | 15.2× | 有明显关联 |
| 亲属 | 180,882 | 29.3% | 10.8× | 数量大、关联显著，但弱于教育/合作 |

**关键区分：**

> 数据的问题不是“关系没有职业信号”，而是“高质量关系信号没有覆盖到足够多的待预测人物”。

---

## 16. 总结、局限与下一步

### 可以得出的结论

1. 邻居职业和 relation type 均有显著预测价值；纯结构远不足以解决 L3 分类。
2. R-GCN、R-GAT、CompGCN 没有形成决定性差距；单纯换 GNN 不是突破路径。
3. 教育/师承关系有超出图连通性的独立职业语义；亲属消融的主要损失来自删去海量连接。
4. L3 瓶颈主要是图稀疏、碎片化、职业信息可达性不足与细粒度长尾的共同作用。

### 局限

- 结论是预测关联，不是因果效应；
- 每个层级独立分层划分，不能把跨层级小数点差异解释为同一批人物变化；
- full-batch structural 实验与主任务输入不同，不用于主结论；
- 当前只包含人物—人物关系，缺少人物文本、机构、作品等异构信息。

### 下一步：优先补数据，而非继续微调 GNN

1. 加入人物简介/职业描述文本；
2. 加入教育机构、雇主、作品、所属领域、组织等实体与边；
3. 补充更丰富的人物关系，尤其教育/师承和职业合作关系；
4. 对低覆盖节点采用“置信度/可达性分层”报告，而非只汇总单一 Accuracy；
5. 在扩展异构图后，再系统评估职业 link prediction。

---

## 附录 A：结果来源与可复现性

| 内容 | 文件 |
|---|---|
| L1 三 seed 汇总 | `runs_report/level1/report_summary.csv` |
| L2 三 seed 汇总 | `runs_report/level2/report_summary.csv` |
| L3 三 seed汇总 | `runs_report/level3/report_summary.csv` |
| 逐 seed 指标 | `runs_report/level*/report_seed_metrics.csv` |
| 数据诊断 | `diagnostics/level3_baseline/summary.json`、`prediction_strata.csv`、`relation_homophily.csv` |
| 批量重跑脚本 | `scripts/run_report_experiments.sh` |
| 汇总脚本 | `scripts/summarize_report_runs.py` |

---

## 附录 B：full-batch structural 探索（不作为主结论）

| 条件 | Accuracy (%) | Macro-F1 (%) |
|---|---:|---:|
| CompGCN structural，sampled train + full eval | 24.23 ± 0.12 | 0.60 ± 0.03 |
| CompGCN structural，true full-batch train + full eval | 9.45 ± 1.69 | 0.03 ± 0.00 |

**说明：** true full-batch 为避免一次全图前向读取训练 seed 自身职业，强制不输入职业特征；因此该实验与主线“可见邻居职业”任务不直接可比。它反映当前 full-batch structural 优化设置未带来改善，需独立诊断，不应作为“全图训练无效”的主证据。

---

## 附录 C：可能被问到的问题

### Q1：为什么职业特征可用，不是泄漏吗？

**答：** 只有训练人物作为邻居时其职业可见；验证、测试和当前 seed 的所有职业层级都设为 `UNKNOWN`。目标人物自身从未读取职业标签。

### Q2：为什么不直接说亲属关系最重要？

**答：** 亲属关系占据极大比例边。删除它会同时删除大量图连接；等规模随机删边也造成近似 Accuracy 损失，所以不能把全部下降归因于亲属语义。

### Q3：relation type 真的有用吗？

**答：** 是。保持拓扑和 relation 频数不变、仅置乱 relation ID 后，L3 Accuracy 从 43.16% 降至 40.89%，Macro-F1 从 14.82% 降至 13.24%。

### Q4：为什么不用更深的 GNN 或更大的 fan-out？

**答：** 完整一、二跳邻域相对默认 15/10 fan-out 没有实质提升；数据诊断表明许多节点本身无法在两跳内接触到已知职业信号。更深传播也未必能跨越图连通分量，且可能过平滑。

### Q5：下一步为什么优先加数据？

**答：** 当前关系一旦存在就有强职业同配性，但其覆盖不足。增加人物文本、教育机构、雇主、作品和更多高质量关系，比继续微调已有 GNN 更可能提升可达职业信息比例。
